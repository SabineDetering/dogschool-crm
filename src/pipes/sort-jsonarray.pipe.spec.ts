import { SortJSONArrayPipe } from './sort-jsonarray.pipe';

describe('SortJSONArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new SortJSONArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
